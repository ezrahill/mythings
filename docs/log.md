# MyThings — Log

- 2026-08-05: Project kicked off by Ezra. Brief confirmed: single-user, local-first, Things 3-style web to-do app. Channel `proj-mythings` and repo created. Plan phase dispatched to Architect.
- 20:55 architect: plan ready (9 steps, ~20 files)
- 21:14 designer: design ready (off-white/accent-blue palette, Inter/SF Pro type stack, 7 sections)
- 21:10 coder: step 1 done. Scaffolded Vite + React + TypeScript project (React Router, Zustand, Dexie installed); build verified.
- 21:14 coder: step 2 done. Added src/types/*.ts and Dexie schema/instance (src/db/schema.ts, db.ts) per the plan's data model; vitest + fake-indexeddb set up, 3 tests passing.
- 21:18 coder: step 3 done. Added src/db/repositories.ts (CRUD + byProject/byArea/byTag/byDateRange queries) for tasks, projects, areas, tags; 15 tests passing.
- 21:20 coder: step 4 done. Added Zustand stores (useTaskStore, useProjectStore, useAreaStore, useTagStore) wired to the repository layer with hydrate/add/update/remove actions; 31 tests passing total.
- 21:25 coder: step 5 done. Added design tokens (src/styles/tokens.css), Sidebar (nav + areas/projects tree), QuickEntry overlay, global useHotkeys (⌘N), App.tsx routing/shell with store hydration on mount, and stub view components for step 7. 57 tests passing total.
- 21:32 coder: step 6 done. Added TaskRow, TaskList, TaskEditor, TagPicker, DatePicker. Found and fixed a real bug along the way: the Zustand stores were writing to Dexie before updating in-memory state, the opposite of the plan's "write-behind" architecture — this caused garbled text in controlled inputs under fast typing (confirmed via TaskEditor tests). Fixed all four stores to update state synchronously first, persist to Dexie after. DatePicker uses quick options + native date input rather than a custom calendar grid (documented in docs/followups.md). 90 tests passing total.
- 21:38 coder: step 7 done. Filled in TodayView (Overdue/Today/This Evening), UpcomingView (date-grouped), ProjectsView/ProjectDetail and AreasView/AreaDetail. Added dates.ts helpers (formatLongDate, groupByDate). Found and fixed another bug: filtering inside a Zustand selector callback returns a new array every render, which broke useSyncExternalStore's snapshot check and caused an infinite render loop in ProjectDetail — fixed by selecting the raw array and filtering in the component body instead (audited all other selectors, only that one had the pattern). TaskEditor is now a fixed-position right-side overlay so it works correctly regardless of which view mounts it. 109 tests passing total.
- 21:44 coder: step 8 done. Added lib/recurrence.ts (nextOccurrence, describeRecurrence — daily/weekly incl. multi-weekday/multi-week intervals, monthly with day-clamping for shorter months) and RecurrencePicker.tsx. Added useTaskStore.completeTask: recurring tasks advance to their next occurrence instead of being marked complete; wired into TaskList's checkbox and TaskEditor's Mark Complete button. 134 tests passing total.
- 21:48 coder: step 9 done. Applied the design's Interactions timings as CSS: row hover (100ms), checkbox/title colour transitions (150ms), detail panel slide-in (200ms), view cross-fade (120ms), Quick Entry scale+backdrop fade (150ms), and a global prefers-reduced-motion override. Deferred the task-complete linger-then-fade, the detail panel's exit transition, and drag-reorder to docs/followups.md — each needs local "recently removed" state timing that's a bigger change than CSS polish, so scoped as targeted follow-ups rather than threading through this pass. 134 tests still passing (CSS-only change).
- 21:55 coder: exercised the built app end-to-end with headless Chromium (puppeteer, since chromium's --virtual-time-budget flag starves IndexedDB callbacks and gave a false "stuck on Loading" reading at first). Found and fixed a real UX bug: tasks created via Quick Entry had no `when` date and no Inbox/all-tasks view exists, so they'd vanish into no view at all. Fixed by defaulting Quick Entry tasks to `when: today`. Verified empty state, add-task, and task-editor-panel flows visually against docs/design.md. Static preview published to /previews/proj-mythings/coder/. 135 tests passing total.
- 21:57 coder: READY — PR https://github.com/ezrahill/mythings/pull/1
- 22:04 reviewer: review done — 2 must-fix (Today/This Evening double-render, overdue recurrence advancing from the wrong date), 3 worth raising, 0 nits. docs/review.md on main.
- 22:23 coder: fixed both must-fixes from docs/review.md. TodayView's Overdue filter now excludes `thisEvening` tasks (matching how `dueToday` already does), so an overdue evening task only shows once. `useTaskStore.completeTask` now advances a recurring task by repeatedly calling `nextOccurrence` until the result is on or after today, instead of stepping once from the task's old `when` — an overdue daily task now catches straight up to today instead of staying overdue by one interval less. Added a regression test for each (137 tests passing total, up from 135). Build and lint clean. Pushed to the PR #1 branch.
- 08:30 coder: found and fixed the preview blank-screen cause — router had no basename and the static build had no base path, so assets/routes broke when served under a subpath (/proj-mythings/coder/). Added basename={import.meta.env.BASE_URL} to BrowserRouter (no-op for a root deploy) and rebuilt the static export with --base=/proj-mythings/coder/. Republished to the preview host, verified in headless Chromium (no console/network errors, Today view renders). Preview now includes both must-fix commits. 137 tests still passing.
